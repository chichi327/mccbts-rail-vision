---
description: 两条 DAG、300ms 延迟、禁止错误的并行聚合
alwaysApply: true
---

# 架构硬约束

- 流水线：人车检测 → 跟踪 → 测距；障碍物检测 → 跟踪 → 高度。量测依赖检测框。
- 端到端事件 < 300ms。只保留最新帧；`drop_if_frame_age_ms` 超时丢帧。禁止加长队列抗延迟。
- 算法直拉主码流。MediaMTX / preview 不得当算法输入。
- 人车不是 obstacle。检测器不算物理量；量测器不准再检一套框。
- 上报状态变化事件 + 独立心跳，禁止每帧全量 POST。
- `core/` 框架团队维护；算法团队只改 `algorithms/<自己的目录>/`。
- 推理不要放进 asyncio 事件循环；asyncio 仅上报/预览 HTTP。
